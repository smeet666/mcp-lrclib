/**
 * Kept in sync with package.json and server.json. The User-Agent embeds it, and
 * LRCLIB asks clients to identify themselves with a version, so it must not drift.
 */
export const PKG_VERSION = "2.0.0";
export const REPO_URL = "https://github.com/smeet666/mcp-lrclib";
